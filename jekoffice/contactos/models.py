from django.db import models

# Create your models here.

# Não existem models aqui porque toda a informação de contactos está presente
# na users app visto que não podem haver contactos sem haver user.
